; If --wrapper isn't passed, then there is no wrapper required and therefore there are no wrapper checks made.
(define ($CONDITION-wrapper$ object)
   object)

(define ($phrase-CONDITION-wrapper$ object)
   object)
